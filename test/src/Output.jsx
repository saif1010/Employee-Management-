import React from 'react';

function Output({ result }) {
    return (
        <div>
            <h2>Output:</h2>
            <p>{result}</p>
        </div>
    );
}

export default Output;
